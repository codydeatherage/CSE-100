#include <iosstream>
#include <limits.h>

using namespace std;

struct graph 
{
	
};

// Prim's algorithim
int main () 
{
	int verticecs, edges;
	scanf("%d, %d", &verticecs &edges);
	int cost[v];
	bool eConnection[v];

	//Set the cost 
	for (int i = 0; i < edges; i++) 
	{
		cost[i] = INT_MAX;
		eConnection[i] = false;
	}

	return 0;
}